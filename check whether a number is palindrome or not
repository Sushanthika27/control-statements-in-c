#include<stdio.h>
int main()
{
   int n,s=0,r,ori;
   printf("enter a number");
   scanf("%d",&n);
   ori=n;
   while(n>0)
   {
        r=n%10;
        s=(s*10)+r;
        n=n/10;
   }
   if(s==ori)
           printf("palindrome");
   else
           printf("not a palindrome");
   return 0;
}
