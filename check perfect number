#include<stdio.h>
int main()
{
   int n,i=1,s=0;
   printf("Enter n");
   scanf("%d",&n);
   while(i<n)
   {
      if(n%i==0)
         s+=i;
      i++;
   }
   if(s==n)
      printf("perfect number");
   else
      printf("not a perfect number");
   return 0;
}
