#include<stdio.h>
int main()
{
   int s=0,ori,n,r;
   printf("enter a number");
   scanf("%d",&n);
   ori=n;
   while(n>0)
   {
           r=n%10;
   if(r%2!=0)
           s=s+r;
           n=n/10;
   }
   printf("sum of odd digits of %d is %d",ori,s);
   return 0 ;
}
