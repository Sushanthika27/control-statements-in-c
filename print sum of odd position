#include<stdio.h>
int main()
{
   int n,c=0,r,s=0,ori,i=0;
   printf("enter a number");
   scanf("%d",&n);
   ori=n;
   while(n>0)
   {
           c=c+1;
           n=n/10;
   }
   if(c%2==0)
  {
          while(ori>0)
          {
                  i=i+1;
                  r=ori%10;
                  if(i%2!=0)
                          s+=r;
                  ori/=10;
          }
  }
                  else
                  {
                          while(ori>0)
                          {
                             i++;
                             r=ori%10;
                             if(i%2==0)
                                s+=r;
                             ori/=10;
                 }


          }
          printf("sum of odd position is :%d",s);
          return 0;
}
