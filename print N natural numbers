#include<stdio.h>
int main()
{
   int i=1,n;
   printf("enter a value of n:\n ");
   scanf("%d",&n);
   while(i<=n)
   {
           printf("\t %d",i);
           i++;
   }
   printf("finished");
   return 0;
}
