#include<stdio.h>
int main()
{
   int n,s=0,r;
   printf("enter a number:");
   scanf("%d",&n);
   while(n>0)
   {
      r=n%10;
      if(r%2==0)
         s=s+r;
       n=n/10;
   }
   printf("sum of even digits :%d",s);
   return 0;
}
